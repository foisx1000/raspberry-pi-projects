# mpv key bindings for the kiosk.
# All default bindings are turned off in kiosk.sh, so random key presses by
# staff cannot pause, seek or mute the video. Only these keys do anything.

ESC   quit
q     quit
CTRL+c quit
